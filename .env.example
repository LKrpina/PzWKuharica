# Example environment variables for PzWKuharica
# Copy this file to `.env` and fill in real values before running or deploying.

# Flask / app
SECRET_KEY=change-me-to-a-secure-random-value
FLASK_ENV=development

# MongoDB (replace with your Atlas/hosted connection string for Render)
# Example local: mongodb://localhost:27017/pzwkuharica
MONGO_URI=mongodb://localhost:27017/pzwkuharica

# Mail settings (used for verification / password reset)
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your-smtp-username
MAIL_PASSWORD=your-smtp-password
MAIL_DEFAULT_SENDER=noreply@example.com
MAIL_TIMEOUT=10
MAIL_SUPPRESS_SEND=False

# Optional: collection name override (app sets a default)
USERS_COLLECTION=users

# Notes:
# - Do NOT commit your real secrets. Use Render's dashboard to set secure environment variables
#   instead of committing them to source control.
# - For Render deployments, set MONGO_URI to your managed MongoDB connection string and set
#   MAIL_* credentials using Render's Environment > Secrets.
